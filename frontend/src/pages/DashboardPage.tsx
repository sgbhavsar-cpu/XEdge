import Typography from '@mui/material/Typography'

// Placeholder home page (XEDGE-510, scaffold only) -- the device list
// (XEDGE-511) replaces this in a later PR.
export default function DashboardPage() {
  return <Typography variant="body1">Signed in. The fleet dashboard lands here next.</Typography>
}
